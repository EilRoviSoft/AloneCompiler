struct vector2 {
	var %x, int32;
	var %y, int32;
}

func @add6432(int64, int32): int64 { # arguments pushes right-to-left
    mov32 %lox, [%spx - 12];
    mov64 %rox, [%spx - 8];
    add64i %asx, %lox, %rox;
    ret;
}
